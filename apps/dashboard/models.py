"""
Dashboard Models
No database models needed - this app provides aggregated data from other apps.
"""
from django.db import models

# This app doesn't require database models
# It aggregates data from other apps (users, tasks, bids, payments, etc.)
